#!/bin/bash

script_name="xcopy"

if [ $# -eq 0 ] || [ ! -f "$1" ]; then
    echo "Error: You must specify a file"
    echo "Usage: $script_name <file>"
    exit 1
fi

wl-copy < "$1"

echo "[*] Content of '$1' copied to clipboard"
